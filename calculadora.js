const somar = require('./formulas/somar')
const subtrair = require('./formulas/subtrair')
const dividir = require('./formulas/dividir')
const multiplicar = require('./formulas/multiplicar')
const calcular = require('./formulas/calcular')

var primeiroNumero = 25;
var segundoNumero = 0;

console.log(calcular(primeiroNumero, segundoNumero, dividir));


